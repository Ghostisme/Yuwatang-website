# 裕和堂 后端项目分析

> 分析路径：`D:\lizhi\workSpace\yuhetang\backend`

## 一、项目概览

| 项 | 内容 |
|---|---|
| 框架 | **FastAdmin**（基于 ThinkPHP 5.0）+ Bootstrap/AdminLTE 后台 |
| 业务核心 | `addons/ygame` 插件 —— 本是**赛事报名系统**（含赛事/组别/订单/团队/证书/成绩） |
| 入口 | `public/api.php`(API)、`public/yuhetangAdmin.php`(后台，改名隐藏 admin)、`public/index.php`(已被改成重定向到 `/h5/`) |
| 数据库 | MySQL，库名 `weilai_daseen_ne`，前缀 `fa_`，34 张表 |
| 实际使用 | 前端只用到了 **2 个接口**：`ygame/banner`（首页视频）、`ygame/store`（店铺图） |

## 二、业务与代码结构

```
application/
├── admin/controller/ygame/   # 后台控制器（Banner/Store/Article/Project/...）
├── admin/model/ygame/        # 13 个模型（Ad/Store/Order/Person/Team/Cert...）
├── api/controller/ygame/     # 12 个 API 控制器
└── common/                   # FastAdmin 公共模块
addons/ygame/
├── service/                  # 业务逻辑层（Ad/Store/Order/Person/Project...）
├── controller/               # Workerman 相关（Index/Work）
├── library/workerman/        # 内置 WebSocket 服务（完整 workerman 源码）
└── uniapp/                   # 配套 uni-app 小程序源码（未使用）
```

**关键观察：严重的前后端错配。** 后端是一套完整的"赛事报名+支付+证书"系统（`fa_ygame_order`、`fa_ygame_record`、`fa_ygame_team`、`fa_ygame_cert`、`fa_ygame_result` 等 10+ 张业务表），但裕和堂官网只用了其中的广告轮播和店铺图。绝大部分控制器、service、模型、表都是**死代码/闲置资产**。

## 三、🔴 严重安全问题（需立即处理）

### 1. 敏感凭证明文硬编码

**(a) 数据库密码**（`application/database.php:14`）
```php
'password' => Env::get('database.password', 'eae946377069a827'),
```
真实生产密码 `eae946377069a827` 直接写死在源码里，而 `.env` 文件**并不存在**。`.gitignore` 虽然忽略了 `.env`，但兜底默认值把密码泄露在了代码中。

**(b) 微信公众号 secret**（`addons/ygame/config.php:11`）
```php
'app_id'     => 'wx3ab6722f20819da0',
'app_secret' => '207a247631d2a7f2974b4f9e6472011c',
```
AppSecret 明文写死，泄露后可被用于伪造网页授权、获取用户 openid/信息。

**(c) SQL dump 随项目分发**（`weilai_daseen_ne.sql`，348K）
- 包含 **admin 账户的密码哈希 + salt**：`admin1` / `admin` 两条记录，密码哈希、salt（`111111`、`9XYW5P`）、token 全部在内。
- 包含完整表结构与部分业务数据。
- 该文件若被提交到仓库或可被 web 访问，等于把后台凭证和库结构直接送出。

> ⚠️ 建议：**立即轮换数据库密码和微信 AppSecret**，删除 SQL dump，把 admin 密码改掉（salt 都泄露了，离线爆破哈希可行）。

### 2. 生产环境开启调试模式

`application/config.php:31`
```php
'app_debug' => Env::get('app.debug', true),   // 兜底默认 true
```
`.env` 不存在时**默认开启 debug**，生产环境会暴露完整堆栈、文件路径、配置变量。应改为兜底 `false`。

### 3. 后台入口与默认凭证风险

- 后台入口 `yuhetangAdmin.php` 虽然改了名（好习惯），但 `install.lock` 已存在意味着系统已装好；FastAdmin 历史上有多个高危 RCE/SQL 注入 CVE，需确认框架版本是否打了补丁（`topthink/framework: dev-master` 锁不住版本）。
- `composer.lock` 被 `.gitignore` 忽略 → 无法复现依赖版本，安全审计困难。

## 四、🟠 前后端契约不一致

前端 `api/index.ts` 定义了 3 个接口调用：

| 前端调用 | 后端是否存在 | 状态 |
|---|---|---|
| `POST /api/cases`（`caseApi`） | ❌ **完全不存在** | 死代码，调用必 404 |
| `GET /api/ygame/banner` | ✅ `api/controller/ygame/Banner.php` | 正常 |
| `GET /api/ygame/store` | ✅ `api/controller/ygame/Store.php` | 正常 |

`cases` 接口在前端定义了却无后端实现，且 `caseApi` 实际从未被组件调用（前端分析时已确认）——属于双向死代码。

另外，前端 axios `baseURL` 写的是 `http://www.yuhetangtcm.com/api.php`，而后端通过 `public/api.php` 入口绑定 api 模块，路径层面是对得上的，但前端绕过了 vite 代理（前一篇已指出）。

## 五、🟡 代码质量问题

1. **Banner 与 Store 几乎是复制粘贴的孪生类**（`api/controller/ygame/Banner.php` 与 `Store.php`、对应 service 的 `Ad.php` 与 `Store.php`、两个 admin 控制器、两个 model）——逻辑完全一致，仅模型不同，可抽公共基类。

2. **图片 URL 拼接重复且不健壮**（`Banner.php:28-32`、`Store.php:28-32`）
   ```php
   $v['image'] = $this->request->domain().$v['image'];
   ```
   - 域名来自请求而非配置，CDN/多域名场景会错。
   - 若 `image` 已是绝对 URL 会拼成 `http://xhttp://y`。
   - 在控制器里循环拼，应下沉到 model 的获取器（FastAdmin 标准做法是 model getter）。

3. **未使用的庞大资产**：`addons/ygame` 里 Workerman（WebSocket）、uniapp 小程序、Order/Person/Project/Result 等 service（共 1157 行）对官网而言全部闲置，徒增攻击面和维护负担。

4. **`.DS_Store` 残留**（`application/api/.DS_Store` 等），非 git 仓库，无 `.gitignore` 生效。

5. **FastAdmin 框架自身老旧**：ThinkPHP 5.0 已 EOL 多年，PHP 要求 `>=7.2` 也偏低。若长期维护，迁移成本会持续上升。

## 六、改进建议（按优先级）

**P0 — 立即（安全）**
1. 轮换数据库密码、微信 AppSecret、admin 密码。
2. 删除 `weilai_daseen_ne.sql`，并从任何 git/备份历史中清除。
3. 创建 `.env`，把 DB 密码、微信 secret、debug 开关全部移入；`config.php`/`database.php` 兜底值改为安全默认（密码空、debug false）。
4. 确认服务器上 `*.sql`、`.env`、`runtime/` 被 web 禁止访问（nginx/location 配置）。

**P1 — 契约与卫生**
5. 删除前端 `caseApi` 死代码（或补后端实现），保持前后端接口清单一致。
6. 提交 `composer.lock`，固定 `topthink/framework` 具体版本，便于安全升级。
7. 初始化 git 仓库，加 `.gitignore`，清理 `.DS_Store`。

**P2 — 重构**
8. Banner/Store 抽公共 trait/基类，图片 URL 用 model 获取器统一处理。
9. 评估是否剥离未使用的报名系统/Workerman/uniapp，降低攻击面；若另有小程序业务则保留并隔离。
10. 中长期评估从 ThinkPHP 5 / FastAdmin 升级路径。

## 七、与前端对照的一句话总结

这是一个**用"赛事报名系统"骨架拼出来的企业官网后端**：核心 FastAdmin + ygame 插件能力很强（报名、支付、证书、WebSocket），但裕和堂实际只用了广告位和店铺图两个最简单的 CRUD 接口，导致大量代码闲置；同时存在**密码/密钥明文硬编码、SQL 泄露 admin 哈希、生产默认开 debug** 三项必须立即修复的安全隐患，前后端接口契约还有一处死链（`/api/cases`）。
