# 裕和堂（Yu Health）前端项目分析

> 分析路径：`D:\lizhi\workSpace\yuhetang\frontend`

## 一、项目概览

这是 **上海裕和堂中医养生品牌** 的官方网站（H5 + PC 双端），数据后端域名为 `yuhetangtcm.com`。

| 项 | 内容 |
|---|---|
| 项目名 | `cashnow`（package.json，与实际业务不符） |
| 技术栈 | Vue 3.5 + TypeScript 5.8 + Vite 7 + Pinia 3 + Vue Router 4 |
| UI 库 | Vant（按需自动导入，但实际页面基本是手写 SCSS） |
| 国际化 | vue-i18n，支持 中文(zh) / 英文(en) / 日文(jp) |
| 路由模式 | Hash 模式（`createWebHashHistory`） |
| 多端方案 | **运行时设备检测 + 双套页面组件**（非响应式 CSS，而是 pc/mobile 两套独立 .vue） |

## 二、目录结构

```
src/
├── api/            # axios 封装 + 接口定义（仅 3 个接口）
├── assets/         # 字体(PingFang、林海体) + 大量图片素材
├── components/     # Header / Footer（全局）
├── locales/        # zh/en/jp 三语文案
├── router/         # index.ts + responsive-routes.ts（核心机制）
├── utils/          # device-detector.ts + rem.ts
├── views/
│   ├── common/     # DeviceRedirect / NotFound
│   ├── pc/         # 8 个 PC 页面
│   └── mobile/     # 8 个移动页面
└── i18n.ts / main.ts / App.vue
```

8 个业务页面：`Home(首页)` `Feature(服务项目)` `Base(蕲艾基地)` `Serve(定制服务)` `Shop(店铺资讯/城市漫步路线)` `Store(查询店铺)` `AboutUs(关于)` `ContactUs(联系)`。

## 三、核心机制

**响应式路由**（`responsive-routes.ts`）：通过 `deviceDetector` 判断 `mobile/tablet/desktop`，用 `defineAsyncComponent` 动态加载 `views/pc/*` 或 `views/mobile/*`，断点 768px。设备类型变化时跳转 `/device-redirect` 做平滑过渡再 replace 回来。

**rem 适配**（`rem.ts`）：手动根据 `clientWidth` 设 `html` font-size，移动端基准 375、PC 基准 1920。注：`vite.config.ts` 里的 `postcss-pxtorem` 已被整体注释，依赖手写 rem 方案。

## 四、值得注意的问题（按重要性排序）

### 🔴 严重 / Bug 级

1. **`index.html` 入口路径错误**（`index.html:11`）
   ```html
   <script type="module" src="/src/main.js"></script>
   ```
   实际文件是 `main.ts`。Vite 开发/构建能容错，但属于明显笔误，应改 `/src/main.ts`。

2. **API `baseURL` 硬编码，未使用环境变量**（`api/request.ts:20`）
   ```ts
   baseURL: "http://www.yuhetangtcm.com/api.php"
   ```
   `.env.development/production` 里定义了 `VITE_API_BASE_URL` 但完全没被 request.ts 使用；`vite.config.ts` 的 `/api` 代理也形同虚设（代码里直接打绝对地址）。导致**跨域、环境隔离、代理全部失效**。

3. **Token 存储键不一致**
   - `request.ts:32` 读 `localStorage.getItem("token")`
   - `router/index.ts:85` 路由守卫读 `localStorage.getItem("auth_token")`
   两套 key，鉴权链路是断的（虽然目前项目实际没有登录页，`requiresAuth` 路由也不存在）。

### 🟠 架构 / 工程问题

4. **package.json `name` 为 `cashnow`**，疑似从模板拷贝未改，建议改为 `yuhetang-web` 之类。

5. **响应式路由实现过度复杂且与 router 重复监听 resize**。`responsive-routes.ts` 的 `createResponsiveWrapper` 内部又 `defineComponent + watch + addEventListener`，与 `router/index.ts:53` 的 `deviceDetector.onResize` 逻辑重叠，且夹带大量 `console.log("触发！！！！！")`、`console.log(1111)` 调试残留。两处都在监听 resize，易产生重复刷新/抖动。

6. **未启用 ESLint / Prettier / 单元测试 / git**（此目录非 git 仓库），代码风格靠人工维持。生产构建产物 `dist/` 也在目录里。

7. **`tsconfig` 关闭了几乎所有严格检查**（`strict: false`、`noUnusedLocals/Parameters: false`），且组件里大量 `as any`（如 Swiper 配置 `paginationOptions as any`），TS 几乎只起装饰作用。

### 🟡 代码质量 / 可维护性

8. **全项目遍布 `console.log` 调试语句**（device-detector、responsive-routes、Header、Home、rem.ts 等），应清理或改用可开关的日志。

9. **`.DS_Store` 多处残留**（src/、assets/、img/、根目录），应加 `.gitignore` 并删除。

10. **国际化文案硬编码在 `switchLanguage` 里**（Header 里 `中文/English/日本语`），且 `menuIndex` 初值写死 `"zh"`，未从 `localStorage.user-locale` 或 `locale.value` 初始化，刷新后高亮可能与实际语言不符。

11. **PC Home 页用绝对像素定位装饰元素**（`.left-icon { top: 1253px }`、`.right-icon { top: 2469px }`），分辨率/内容变化就会错位，脆弱。

12. **`components.d.ts`** 由 `unplugin-vue-components` 生成，但只声明了 Footer/Header/RouterLink/RouterView——说明 Vant 组件虽配了 resolver，实际几乎没用上（可考虑是否还需要 Vant 依赖）。

13. **`api/index.ts` 仅 3 个接口**（`caseApi`/`getHomeBanner`/`getStoreBanner`），而 `caseApi` 似乎没被调用，大部分页面内容是写死的静态资源，前后端联动很薄。

## 五、改进建议（优先级）

1. **修 `index.html` 入口** → `/src/main.ts`。
2. **统一 API 基址**：`request.ts` 改用 `import.meta.env.VITE_API_BASE_URL`，并让 `vite.config.ts` 代理生效（或直接保留绝对地址但走 env）。
3. **统一 token key**，或暂时移除未启用的鉴权逻辑。
4. **清理 `console.log` 与 `.DS_Store`**，初始化为 git 仓库并加 `.gitignore`。
5. **简化响应式路由**：`createResponsiveWrapper` 内部不需要再嵌一层 `defineComponent` + resize 监听，信任 router 层的设备切换即可；或干脆用 vueuse 的 `useMediaQuery` + 单一 `v-if`。
6. **改 `package.json` name**；开启 `strict: true` 逐步补类型。
7. **Header 语言菜单**：`menuIndex` 用 `locale.value` 派生，去掉硬编码。

## 六、一句话总结

这是一个**功能完整、视觉精良的企业品牌官网**（中医养生、三语、PC/H5 双端），业务页面齐全；但**工程化偏弱**：环境变量/代理/鉴权链路未打通、TS 严格度低、调试日志和 macOS 残留文件遍地、响应式路由机制存在重复监听与过度封装。短期补丁（前 4 条）即可显著提升健壮性，长期建议做一次工程化治理（lint + git + env + 类型收紧）。
